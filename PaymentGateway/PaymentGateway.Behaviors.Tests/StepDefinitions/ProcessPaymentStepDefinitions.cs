namespace PaymentGateway.Behaviors.Tests.StepDefinitions
{
    [Binding]
    public sealed class ProcessPaymentStepDefinitions
    {
        // For additional details on SpecFlow step definitions see https://go.specflow.org/doc-stepdef

        [When("Process Payment")]
        public void WhenProcessPayment()
        {
            //TODO: implement act (action) logic with controllers

            throw new PendingStepException();
        }

        [Then("Create new payment operation")]
        public void ThenCreateNewPayment()
        {
            //TODO: implement assert (verification) logic with controllers

            throw new PendingStepException();
        }
    }
}